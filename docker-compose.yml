version: '3.8'
services:
  api:
    build: ./phx_api
    ports:
      - "4000:4000"
    volumes:
      - .:/phx_api
    depends_on:
      - db
    env_file: ./.env
  db:
    image: postgres:latest
    env_file: ./.env
    ports:
      - "5432:5432"
